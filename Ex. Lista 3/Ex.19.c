#include <stdlib.h>
#include <stdio.h>
#include <math.h>

int main(void)
{

    int lado1, lado2, hipotenusa, a, b, c, i;

    if ()
        for (i = 1; i <= 500; i++)
        {
            for (a = 1; a <= 500; a++)
                for (b = 1; b <= 500; b++)
                    for (c = 500; c >= 1; c--)

                        lado1 = sqrt(pow(c, 2) - pow(b, 2));
            lado2 = sqrt(pow(c, 2) - pow(a, 2));
            hipotenusa = sqrt(pow(a, 2) + pow(b, 2));
        }

    return 0;
} // inacabado;